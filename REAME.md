This repo contains a simple implementation of a client that has the following characteristics:
    -Uses a MQTT client (paho) to communicate with a broker (mosquitto).
    -Uses an interface to the LoRa radio RFM69HCW, to gather data from a WSN and send it to the broker.
